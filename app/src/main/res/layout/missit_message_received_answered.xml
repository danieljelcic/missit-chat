<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@null">


    <include layout="@layout/message_received"
        android:id="@+id/currMessageContainer" />

    <include layout="@layout/message_sent"
        android:id="@+id/missitResponseContainer"
        android:layout_below="@+id/currMessageContainer"
        android:layout_height="wrap_content"
        android:layout_width="match_parent" />

   <!-- <include android:id="@+id/missitResponseContainer"
        android:layout_above="@+id/currMessageContainer"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content" />

    <include android:id="@+id/currMessageContainer"
        android:layout_below="@+id/missitResponseContainer"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content" />-->

</RelativeLayout>